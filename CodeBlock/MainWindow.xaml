<Window x:Class="CodeBlock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CodeBlock"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800" FontSize="20">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <TextBox Name="InputTextBox" TextWrapping="NoWrap" AcceptsReturn="True" FontFamily="Courier New"/>
        </ScrollViewer>
        <Button Click="Button_Click" Grid.Row="1" Content="next" Margin="0 5 0 0"/>
    </Grid>
</Window>
